// This file is just to make VSCode shut up. If the tests folder isn't included in a tsconfig.json specifying the target it assumes 
// ES2015 or something. I could also add tests to the regular tsconfig.json, but then it would compile the tests into dist
{
  "include": [
    "./**/*",
  ],
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "moduleResolution": "node",
    "sourceMap": true,
  }
}
